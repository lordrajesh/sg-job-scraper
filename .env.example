# Scraper settings
MIN_DELAY=2
MAX_DELAY=5
MAX_PAGES=5
OUTPUT_FORMAT=csv
HEADLESS=true

# Note: GitHub Actions uses Vercel deployment (secrets: VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID)
# For local development, Vercel CLI handles deployment automatically
